import { SpText } from 'common';
import styled from 'styled-components';

export const NotifyText = styled(SpText)`
  display: flex;
  flex-direction: column;
  min-width: 30rem;
  min-height: 10rem;
  border-radius: 5rem;
  padding: 2rem;
`;

